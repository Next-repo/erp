{% if condition is defined ? condition : is_granted('ROLE_CAN_ADD') %}

	{#<button 
		type="button"
		class="btn {{btn ?? 'btn-primary'}} {{className}} d-flex align-items-center"
		data-bs-toggle="modal"
		data-bs-target="#{{target is defined ? target : 'addModal'}}"
		data-url="{{ path(path, {'id': data.id}) }}"
		>
	
		<i class="iconoir-{{icon is defined ? icon : 'plus'}}"></i>
		{{text is defined ? text : ''}}

	</button>#}

	<button 
		type="button"
		aria-controls="offcanvasEnd" 
		class="btn {{btn ?? 'btn-outline-primary d-flex align-items-center btn-sm'}} {{className}}"
		data-bs-target="#{{target is defined ? target : 'addOffcanvas'}}" 
		data-url="{{ path(path, {'id': data.id}) }}"
		data-bs-toggle="offcanvas">

		<i class="ti ti-{{icon is defined ? icon : 'plus'}}"></i>
		{{text is defined ? text : ''}}

	</button>

{% endif %}
